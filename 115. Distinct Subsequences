hard ---> 115. Distinct Subsequences---->https://leetcode.com/problems/distinct-subsequences/
resursive solution-->O(n*(2^n))-->gives you TLE;
class Solution {
    public void helper(String s,String t,int i,StringBuilder sb,int []count){
        if(i==s.length()){
            String str=sb.toString();
            if(str.equals(t)) count[0]++;
            return;
        }
        sb.append(s.charAt(i));
        helper(s,t,i+1,sb,count);
        sb.deleteCharAt(sb.length()-1);
        helper(s,t,i+1,sb,count);

    }
    public int numDistinct(String s, String t) {
        int []count=new int[1];
        helper(s,t,0,new StringBuilder(),count);
        return count[0];
    }
}
dp Solution--->O(m*n)
class Solution {
public int numDistinct(String s, String t) {
        int m = s.length(), n = t.length();
        int[][] dp = new int[m + 1][n + 1];
        for(int i = 0; i < m; i ++){
            dp[i][0] = 1;
        }
        for(int i = 1; i <= m; i ++){
            for(int j = 1; j <= n; j ++){
                if(s.charAt(i - 1) == t.charAt(j - 1)){
                    dp[i][j] = dp[i - 1][j - 1] + dp[i - 1][j];
                }else{
                    dp[i][j] = dp[i - 1][j];
                }
            }
        }
        return dp[m][n];
    }
}
